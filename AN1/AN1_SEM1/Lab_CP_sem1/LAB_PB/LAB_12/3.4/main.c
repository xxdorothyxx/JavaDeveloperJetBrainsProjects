#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#define WORD_SIZE 30
char print(char *a){
    a=(char*)malloc(WORD_SIZE*sizeof(char));
    if(
    else
}
int main() {
    printf("Hello, World!\n");
    return 0;
}