#include <stdio.h>
#include <stdlibs.h>
#include <structures.h>
#include <functions.h>
// 3.3+3.9 homework

int main() {
    int n;
    printf("Input how many students you want to read:\nn=");
    scanf("%d",&n);
    for (int i = 0; i < ; ++i) {
        student * readStudents(i);
    }

    return 0;
}